// Exercise 2.
// --------------------------------------------------------


function power(base, exp){
  return Math.pow(base, exp);
}

var result = power(2, 4);
console.log(result); // 16, 2 to the 4th power
